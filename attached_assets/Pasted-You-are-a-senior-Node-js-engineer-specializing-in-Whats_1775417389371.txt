You are a senior Node.js engineer specializing in WhatsApp automation using the Baileys library.

I have a WhatsApp pairing system that is NOT working. You must FIX it and return FULLY WORKING CODE.

STRICT REQUIREMENTS (DO NOT SKIP):

1. USE LATEST BAILEYS

* Use @whiskeysockets/baileys
* Use multi-file auth state (useMultiFileAuthState)
* Ensure compatibility with latest WhatsApp Web version

2. FIX PAIRING CODE FLOW (CRITICAL)

* Generate pairing code using:
  sock.requestPairingCode(phoneNumber)
* Format phone number correctly (country code, no + or spaces)
* Return pairing code as: XXXX-XXXX (formatted)

3. CONNECTION LOGIC

* Handle connection.update event properly

* Detect:

  * "connecting"
  * "open" → SUCCESS
  * "close" → RECONNECT unless logged out

* Use this logic:

  * If disconnected (not logged out) → auto reconnect
  * If logged out → clear session and restart pairing

4. USER NOTIFICATION (MANDATORY)
   When pairing starts, log/send:

"Enter this WhatsApp pairing code on your phone: XXXX-XXXX"

Also show:

* "Generating pairing code..."
* "Waiting for device confirmation..."
* "Connected successfully ✅"

5. SESSION STORAGE

* Save auth state using:
  useMultiFileAuthState("./session")
* Persist session automatically
* On restart → reconnect WITHOUT pairing if session exists

6. ERROR FIXES
   Fix ALL common issues:

* pairing code not generating
* undefined sock errors
* session not saving
* immediate disconnect after pairing
* version mismatch

7. COMPLETE CODE ONLY
   Return:

* Full working index.js
* No pseudo code
* No explanations unless necessary
* Code must run with:
  npm install @whiskeysockets/baileys

8. ADD DEBUG LOGS
   Add console logs for:

* connection status
* pairing code
* errors

9. FINAL RESULT MUST:

* Start server
* Ask for phone number
* Generate pairing code
* Wait for user to enter code on phone
* Detect success
* Stay connected

DO NOT GIVE THEORY.
DO NOT SKIP ANY PART.
RETURN CLEAN, RUNNABLE CODE ONLY.
