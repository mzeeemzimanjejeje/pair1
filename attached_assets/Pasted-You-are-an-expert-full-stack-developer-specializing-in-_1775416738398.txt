You are an expert full-stack developer specializing in WhatsApp Web API integrations (Baileys / Meta Cloud API).

TASK:
Fix and improve my WhatsApp pairing system to ensure it is fully functional, stable, and user-friendly.

REQUIREMENTS:

1. FIX PAIRING LOGIC

* Debug and correct the pairing code generation process.
* Ensure the pairing code is always generated correctly and matches WhatsApp standards.
* Handle expired or invalid pairing codes gracefully.
* Prevent duplicate sessions or crashes during pairing.
* Ensure reconnection logic works if pairing fails.

2. USER NOTIFICATION SYSTEM

* When a user initiates pairing, immediately display a clear notification:
  "Enter the WhatsApp pairing code sent to your device."
* Show real-time status updates:

  * "Generating pairing code..."
  * "Waiting for confirmation..."
  * "Successfully connected ✅"
  * "Connection failed ❌, retrying..."

3. AUTO CONFIRMATION DETECTION

* Detect when the user successfully enters the pairing code on their phone.
* Automatically update the UI/backend status to "Connected".
* Store session securely after successful pairing.

4. ERROR HANDLING

* Handle these cases:

  * Network failure
  * Invalid code
  * Timeout
  * WhatsApp disconnect
* Provide meaningful error messages to the user.

5. SESSION MANAGEMENT

* Save session credentials securely (JSON or database).
* Allow auto-reconnect without requiring a new pairing code.
* Clean up old/invalid sessions.

6. BACKEND IMPLEMENTATION

* Use Node.js with Baileys (preferred) or WhatsApp Cloud API.
* Write clean, modular, production-ready code.
* Include comments explaining key parts.

7. FRONTEND (OPTIONAL BUT PREFERRED)

* Display pairing code clearly.
* Show connection status visually (loading, success, error).
* Simple responsive UI.

8. SECURITY

* Do not expose session credentials publicly.
* Validate all inputs.
* Prevent abuse/spam pairing attempts.

OUTPUT FORMAT:

* Provide FULL working code (server + optional frontend).
* Include setup instructions.
* Ensure code runs without errors.

GOAL:
A fully working WhatsApp pairing system where:

* User gets a pairing code
* User is clearly notified to enter it
* System detects confirmation
* Connection is established and saved successfully

Make sure everything is tested and production-ready.
