{
  "version": 2,
  "installCommand": "pnpm install --no-frozen-lockfile --ignore-scripts=false",
  "buildCommand": "pnpm --filter @workspace/pairing-site run build && mkdir -p public && cp -r ../pairing-site/dist/public/. ./public/",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api(.*)", "destination": "/api/index" },
    { "source": "/((?!assets|favicon|opengraph).*)", "destination": "/index.html" }
  ]
}
