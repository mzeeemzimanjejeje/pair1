{
  "version": 2,
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "cd ../.. && pnpm install --no-frozen-lockfile --ignore-scripts=false && pnpm --filter @workspace/pairing-site run build && mkdir -p artifacts/api-server/public && cp -r artifacts/pairing-site/dist/public/. artifacts/api-server/public/",
  "outputDirectory": "public",
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api(.*)", "destination": "/api/index" },
    { "source": "/((?!assets|favicon|opengraph).*)", "destination": "/index.html" }
  ]
}
